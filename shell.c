#include "shell.h"
#include <stdio.h>

/**
 * main - Entry point for the shell program.
 *
 * Return: Always 0.
 */
void shell(void);

int main(void)
{
	/* Call shell function */
	shell();

	return (0);
}
